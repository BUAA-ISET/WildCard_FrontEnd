# PR Draft

Title: feat(user): switch frontend user flow to real backend APIs

Description:
- route user auth/profile requests to backend by default
- normalize backend user payloads in user API layer
- keep room and rule modules on mock mode
- update user view test config for the new user mock toggle
